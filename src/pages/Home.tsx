import Header from "../components/common/Header";

const Home: React.FC = () => {
  return (
    <div className="Home">
      <Header name="홍길동" point={30} />
    </div>
  );
};

export default Home;
